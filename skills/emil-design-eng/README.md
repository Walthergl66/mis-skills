# Emil Design Eng

Use this skill when an agent needs design-engineering judgment for UI polish, motion, interaction details, component feel, layout rhythm, and product-quality frontend decisions.

Canonical instructions live in `SKILL.md`. Keep agent-specific usage notes in `adapters/<agent>` instead of changing the universal skill unless the behavior should apply everywhere.
