# Claude Code Adapter

Run `npm run sync -- claude` to copy universal skills into `adapters/claude/skills`.

Use this directory for Claude Code-specific commands, hooks, or memory files. Keep reusable skill instructions in `.agents/skills`.
