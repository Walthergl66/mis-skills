# Codex Skills

Codex uses the canonical universal skills directly from:

```txt
.agents/skills
```

This folder exists only to make the adapter layout explicit and easy to scan. Do not duplicate Codex skills here unless a future Codex-specific compatibility layer needs it.

Run `npm run validate` to verify the real skills.
