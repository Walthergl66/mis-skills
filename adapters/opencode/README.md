# OpenCode Adapter

Run `npm run sync -- opencode` to copy universal skills into `adapters/opencode/skills`.

Use this directory for OpenCode-specific configuration. Keep portable skill content in `.agents/skills`.
