# Cursor Adapter

Run `npm run sync -- cursor` to copy universal skills into `adapters/cursor/skills`.

Use this directory for Cursor-specific rules, project prompts, or import glue while keeping canonical skill content in `.agents/skills`.
