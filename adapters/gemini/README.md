# Gemini CLI Adapter

Run `npm run sync -- gemini` to copy universal skills into `adapters/gemini/skills`.

Use this directory for Gemini CLI-specific prompt wrappers or command aliases. Keep the source skill in `.agents/skills`.
