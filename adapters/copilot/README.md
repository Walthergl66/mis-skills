# GitHub Copilot Adapter

Run `npm run sync -- copilot` to copy universal skills into `adapters/copilot/skills`.

Use this directory for Copilot instructions or repository-level prompt files that reference the universal skill inventory.
